# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ML model weights — too large for git (>100MB), fetched via scripts/fetch-models.mjs
src-tauri/models/*.bin
src-tauri/models/*.gguf
src-tauri/models/*.onnx

# Local-only tooling state
.sidebar/
.claude/settings.local.json

# Local test build from `pnpm release` (overwritten every run, never shipped)
/release/
